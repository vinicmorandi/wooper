generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = file:./dev.db
}

model Usuario{
    id Int @id @default(autoincrement())
    nome String
    senha String
    email String
    tipo Int
}